---
title: Popover
description: A floating panel anchored to a trigger element, for non-modal rich content.
---

import { Tabs, TabItem } from "@astrojs/starlight/components";
import { PopoverDemoReact } from "../../../components/examples/popover/PopoverDemoReact.tsx";
import PopoverDemoVue from "../../../components/examples/popover/PopoverDemoVue.vue";

A **Popover** displays rich floating content anchored to a trigger. Unlike Dialog, it is non-modal: clicking outside closes it. It uses the floating positioning engine built into forge-ui.

## Example

<Tabs>
  <TabItem label="React">
    <PopoverDemoReact client:visible />
  </TabItem>
  <TabItem label="Vue">
    <PopoverDemoVue client:visible />
  </TabItem>
</Tabs>

## Usage

<Tabs>
  <TabItem label="React">
    ```tsx
    import { Popover } from "@forge-ui/react";

    export function MyPopover() {
      return (
        <Popover.Root>
          <Popover.Trigger>Settings</Popover.Trigger>
          <Popover.Portal>
            <Popover.Content>
              <Popover.Title>Appearance</Popover.Title>
              <Popover.Description>Customise your interface.</Popover.Description>
              <Popover.Close>×</Popover.Close>
            </Popover.Content>
          </Popover.Portal>
        </Popover.Root>
      );
    }
    ```
  </TabItem>
  <TabItem label="Vue">
    ```vue
    <script setup lang="ts">
    import {
      PopoverRoot, PopoverTrigger, PopoverPortal,
      PopoverContent, PopoverTitle, PopoverDescription,
      PopoverClose,
    } from "@forge-ui/vue";
    </script>

    <template>
      <PopoverRoot>
        <PopoverTrigger>Settings</PopoverTrigger>
        <PopoverPortal>
          <PopoverContent>
            <PopoverTitle>Appearance</PopoverTitle>
            <PopoverDescription>Customise your interface.</PopoverDescription>
            <PopoverClose>×</PopoverClose>
          </PopoverContent>
        </PopoverPortal>
      </PopoverRoot>
    </template>
    ```
  </TabItem>
</Tabs>

## Placement

```tsx
<Popover.Root placement="bottom-start">...</Popover.Root>
```

Available placements: `top`, `top-start`, `top-end`, `bottom` (default), `bottom-start`, `bottom-end`, `left`, `left-start`, `left-end`, `right`, `right-start`, `right-end`.

## Styling

```css
[data-forge-scope="popover"][data-forge-part="content"] {
  background: white;
  border: 1px solid #e2e8f0;
  border-radius: 8px;
  padding: 1rem;
  box-shadow: 0 10px 30px rgba(0 0 0 / 0.15);

  /* Fade in when positioned */
  opacity: var(--forge-revealed, 0);
  transition: opacity 0.15s ease;
}
```

## API Reference

### Popover.Root

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `open` | `boolean` | — | Controlled open state |
| `defaultOpen` | `boolean` | `false` | Initial open state (uncontrolled) |
| `onOpenChange` | `(open: boolean) => void` | — | Fires on state change |
| `placement` | `Placement` | `"bottom"` | Floating placement |
| `offset` | `number` | `8` | Distance from anchor in px |
| `modal` | `boolean` | `false` | Enable modal behaviour (focus trap + scroll lock) |
| `onPointerDownOutside` | `(e: PointerEvent) => void` | — | Pointer down outside |
| `onEscapeKeyDown` | `(e: KeyboardEvent) => void` | — | Escape key |

### Popover.Anchor

An optional element to use as the positioning reference instead of the trigger:

```tsx
<Popover.Root>
  <Popover.Trigger>Open</Popover.Trigger>
  <Popover.Anchor asChild>
    <div className="anchor-target" />
  </Popover.Anchor>
  <Popover.Portal>
    <Popover.Content>Positioned relative to the anchor</Popover.Content>
  </Popover.Portal>
</Popover.Root>
```

### Popover.Arrow

A renderless compound that merges arrow positioning props onto its child:

```tsx
<Popover.Content>
  Content here
  <Popover.Arrow>
    <svg width="10" height="5" viewBox="0 0 30 10">
      <polygon points="0,0 30,0 15,10" />
    </svg>
  </Popover.Arrow>
</Popover.Content>
```

### Popover.Content

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `forceMount` | `boolean` | `false` | Keep in DOM when closed (for exit animations) |
| `asChild` | `boolean` | `false` | Merge props onto child element |

### Popover.Portal

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `container` | `HTMLElement` | `document.body` | Portal target |
| `forceMount` | `boolean` | `false` | Keep portal when closed |

## Data attributes

| Attribute | Element | Values |
|-----------|---------|--------|
| `data-forge-scope` | All parts | `"popover"` |
| `data-forge-part` | All parts | `"trigger"` \| `"anchor"` \| `"positioner"` \| `"content"` \| `"title"` \| `"description"` \| `"close"` \| `"arrow"` |
| `data-state` | `trigger`, `content` | `"open"` \| `"closed"` |
| `data-side` | `content`, `positioner` | `"top"` \| `"bottom"` \| `"left"` \| `"right"` |
| `data-placement` | `positioner` | Full placement string |

## CSS variables

| Variable | Description |
|----------|-------------|
| `--forge-popover-content-transform-origin` | Computed transform origin for scale animations |
| `--forge-revealed` | `1` when positioned and ready to show |

## Accessibility

- `Popover.Content` renders with `role="dialog"` when modal, otherwise no role
- `Popover.Trigger` gets `aria-expanded` and `aria-haspopup="dialog"`
- Focus moves inside on open; returns to trigger on close
- `Escape` closes the popover
